# Оператор `in`

Проверка наличия документа в каталоге.

```parser3
in "/каталог/"
```

Возвращает результат «истина / ложь» в зависимости от того, находится ли текщий документ в указанном каталоге.

#### Пример
```parser3
^if(in "/news/"){
	Мы в разделе новостей
}{
	<a href="/news/">Новости</a>
}
```
