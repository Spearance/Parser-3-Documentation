# Статическое поле `PARSER_VERSION`

Получение версии Parser.

```parser3
$env:PARSER_VERSION
```

Такая конструкция возвращает полную версию Parser с указанием платформы.

Например:

`3.4.1 (compiled on i386-pc-win32)`
