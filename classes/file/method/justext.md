# Статический метод `:justext`

Расширение имени файла.

```parser3
^file:justext[filespec]
```

Метод принимает в качестве параметра полный путь к файлу `filespec` и возвращает раcширение имени файла без точки.

#### Пример
```parser3
^file:justext[/a/some.tar.gz]
```

выдаст: **gz**
