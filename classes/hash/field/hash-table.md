# Хеш вместо таблицы

Использование.

```parser3
$хеш.fields
```

Вернет сам хеш.

Для большей взаимозаменяемости таблиц и хешей поле `fields` хранит ссылку на сам хеш. См. `[table.fields](/class/table/)`.
