# Статический метод `fields`

Список полей объекта.

```parser3
^reflection:fields[класс]
```
```parser3
^reflection:fields[объект]
```

Для класса метод возвращает хеш со списком статических полей, для объекта — со списком динамических полей. 
