# Статический метод `:pow`

Возведение числа в степень.

```parser3
^math:pow(число;степень)
```

Метод возводит число в степень.

#### Пример
```parser3
^math:pow(2;10)
```

Получаем: **1024** (2^10^ = 1024). 
