# Методы `int`, `double`

Преобразование даты в число ***[3.5.0]***

```parser3
^date.int[]
```
```parser3
^date.double[]
```

Методы возвращают числовое значение объекта класса `date`, равное числу суток с EPOCH (01.01.1970 00:00:00, UTC) до даты, заданной в объекте. 
