# Поля

Объекта класса `regex`.

## Текст шаблона

```parser3
$шаблон.pattern
```

Поле содержит строку с исходным текстом регулярного выражения.

## Опции

```parser3
$шаблон.options
```

Поле содержит строку с исходным текстом опций. 
