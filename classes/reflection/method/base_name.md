# Статический метод `:base_name`

Имя родительского класса объекта.

```parser3
^reflection:base_name[класс]
```
```parser3
^reflection:base_name[объект]
```

Метод возвращает имя базового класса объекта или класса (если он есть) либо пустую строку. 
