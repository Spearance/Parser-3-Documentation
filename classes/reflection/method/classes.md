# Статический метод `:classes`

Список классов.

```parser3
^reflection:classes[]
```

Метод возвращает хеш со списком всех классов, доступных на момент вызова. Ключами хеша являются имена классов, значениями могут быть строки methoded (для классов, содержащих методы) или `[void](/class/void/)`.
