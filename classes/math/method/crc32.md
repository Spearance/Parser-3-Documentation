# Статический метод `:crc32`

Подсчет контрольной суммы строки.

```parser3
^math:crc32[строка]
```

Метод подсчитывает контрольную сумму (CRC32) для переданной строки. Затем выдает ее в виде целого числа. 
