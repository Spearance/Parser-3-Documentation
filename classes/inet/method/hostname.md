# Статический метод `:hostname`

Имя хоста. ***[3.5.0]***

```parser3
^inet:hostname[]
```

Метод возвращает текущее имя хоста (host name) — сетевой идентификатор узла, на котором выполняется программа. 
