# Поле `mode`

Режим работы. ***[3.5.0]***

```parser3
$status:mode
```

Возвращает режим, в котором работает Parser. Возможные значения: **cgi**, **console**, **mail**, **httpd**, **apache**, **isapi**. 
