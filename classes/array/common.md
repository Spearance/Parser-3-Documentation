## Кратке описание

 > Массив считается определенным `def`, если в нем есть хотя бы один инициализированный элемент. Числовым значением массива является количество инициализированных элементов (значение, возвращаемое методом `^массив.count[]`).
